// TODO: copy from guitar-navigation repo.

export const Fretboard = ({ numFrets, numStrings }: { numFrets: number, numStrings: number }) => {
    return `Fretboard with ${numStrings} strings, ${numFrets} frets.`
}